# 前端重构记录

## 设计与范围

- APP 参考：[Electra EV Charging](https://dribbble.com/shots/27582487-Electra-EV-Charging-Mobile-App-Smart-Electric-Vehicle-App)，采用深色、紫色渐变、车辆卡片、能量光环和底部导航。
- 大屏目标参考：[可视化数据大屏设计](https://dribbble.com/shots/19917694-)，根据用户随后提供的截图，已重做为紫色数字地球、中国地图、左侧指标与趋势、右侧两列四行图表。
- 初版未能从 Dribbble 取得原图；大屏第二版以用户在会话中提供的截图上半屏为视觉依据。APP 仍待对应原图核对。
- 保留原生前端与同源 API。未修改后端、数据库结构或部署配置。新增独立移动端和大屏样式表、本地车辆 SVG；将修改的 HTML/JS 格式化为可维护源码。
- 未登录余额显示待连接，最近订单合计来自订单查询；充电百分比明确标识为演示值，不代表车辆实测。大屏地球为全域视觉背景，业务数据仍来自现有站点与 UrbanEV 小时样本，深圳标记显示空闲充电枪数量。

## 验证（2026-09-05）

- JavaScript 语法检查通过。
- Playwright 在本地 FastAPI + PostgreSQL 上验证手机号登录、充值、选枪、启动充电、停止结算、订单查询。
- 大屏导航、站点搜索、12 个 UrbanEV 站点节点、刷新和管理员登录弹窗通过。
- 移动端 320 / 390 / 430 px，大屏 390 / 1024 / 1440 / 1920 px 检查无水平溢出，浏览器无 JavaScript 异常。
- 针对隐藏状态初始化地图的回归检查与排行第四行可见性检查通过。
- 验证创建了一个独立开发用户及一笔充值、充电订单，审计数据保留。未运行后端全量冒烟脚本；本次后端没有变更。
- 修改前源码备份、浏览器验证脚本及截图保存在 `.runtime/ui-refactor/`。本目录不是 Git 仓库，使用备份进行差异检查。

## 大屏参考图重做

- 删除初版居中标题、横向四卡片、青蓝网络网格和面板边框。
- 按参考图采用左 57% / 右 43% 布局：紫色地球、中国省界、点阵光晕、青色切角标题条、24 小时面积图，以及双列四行的饼图、同心圆、双环、站点监测和排行。
- 站点排行可切换重点站点，重点站点卡可进入过滤后的站点列表；保留订单分析、管理员登录、刷新和全屏。
- 地球为本地 SVG，不依赖外部图片请求；行政边界做了视觉放大，用于展示而非测绘。
- 第二版修改前备份、验收脚本及截图位于 `.runtime/ui-refactor-v2/`；公网入口沿用 Funnel 的 8443 端口。
- 第二版公网验收通过：1920×1080、1440×900、1366×768、390×844 无水平溢出或图表裁切；站点联动与搜索、订单视图、趋势悬停、管理员弹窗、全屏、刷新通过，浏览器无 JavaScript 异常。移动端首页回归通过，本次未修改移动端文件。

## 站点态势与订单分析统一设计

- 站点态势：深圳区域地图、真实坐标节点、空闲设备环图、能源参数、设备规模排行与可搜索站点清单；地图、柱图、清单联动选中站点。
- 订单分析：地球背景与交易指标、公开七日营收趋势、24 小时典型日占用率，以及管理员订单状态和交易清单。未登录时显示连接入口，过期会话恢复为公开视图。
- 新增 `dashboard-details.css` / `dashboard-details.js` 隔离两屏样式与渲染，沿用同一次服务端查询快照。总览样式和 APP 文件未修改。
- 深圳边界来自 https://geo.datav.aliyun.com/areas_v3/bound/440300_full.json，生成本地 `assets/shenzhen-region.svg`；地图仅绘制深圳 UrbanEV 站点，区域外平台站点可查看详情。
- 修改前备份、验证脚本与截图保存在 `.runtime/ui-refactor-v3/`。
